const prodHeader = document.querySelector('.products-header');
const prodDivide = document.querySelector('.products-divider');
const productsContain = document.querySelector('.products-container');

resizeProducts();

// function resizeProducts() {
//   const newWidth = productsContain.clientWidth;
//   console.log(newWidth);
//   prodHeader.style.width = `${newWidth}px`;
// }
